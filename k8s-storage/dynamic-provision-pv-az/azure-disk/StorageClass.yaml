apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: managed-disk-sc
provisioner: disk.csi.azure.com
parameters:
  skuName: Standard_LRS   # or Premium_LRS
reclaimPolicy: Delete
volumeBindingMode: WaitForFirstConsumer
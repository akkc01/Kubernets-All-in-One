apiVersion: v1
kind: ResourceQuota
metadata:
  name: nginx-quota
  namespace: nginx  # iss namespace se ye resource quota bind hoga

spec:
  hard:
    pods: "10"
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "2"
    limits.memory: 4Gi
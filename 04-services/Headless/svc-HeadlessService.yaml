apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: sql-app
spec:
  clusterIP: None   #  headless service
  selector:
    app: sql-app
    environment: dev
  ports:
  - name: mysql
    protocol: TCP
    port: 3306
    targetPort: 3306
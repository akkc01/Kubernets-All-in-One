apiVersion: v1
kind: Service
metadata:
  name: nginx-svc-LB
  namespace: nginx-ns
spec:
  type: LoadBalancer
  selector:
    app: nginx
    environment: dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
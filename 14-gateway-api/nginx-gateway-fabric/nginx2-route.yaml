apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx2-route
  namespace: ngf-test

spec:
  parentRefs:
    - name: nginx-gateway

  hostnames:
    - nginx2.theakkc.space

  rules:
    - backendRefs:
        - name: nginx-2
          port: 80
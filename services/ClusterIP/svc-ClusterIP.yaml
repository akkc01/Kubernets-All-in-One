apiVersion: v1
kind: Service
metadata:
  name: nginx-svc-ClusterIp
  namespace: nginx-ns
spec:
  type: ClusterIP
  selector:
    app: nginx
    environment: dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80